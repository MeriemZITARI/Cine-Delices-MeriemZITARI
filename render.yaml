services:
  - type: web
    name: backend
    env: docker
    plan: free
    rootDir: backend
    dockerfilePath: ./Dockerfile
    envVars:
      - fromGroup: CineDelice-Prod-envVars


  - type: web
    name: frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    rootDir: front
    envVars:
      - fromGroup: CineDelice-Prod-envVars

databases:
  - name: cine-db
    plan: free
